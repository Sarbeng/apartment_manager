<div>
    {{-- Close your eyes. Count to one. That is how long forever feels. --}}
    <button type="{{$type}}" class="bg-blue-900 px-4 h-10 w-full rounded text-white">
        <span>{{ $label }}</span>
    </button>
</div>
